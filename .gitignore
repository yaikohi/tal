# --- Security (CRITICAL) ---
# The private key used to decrypt your secrets. NEVER COMMIT THIS.
key.txt
*.age

# Unencrypted secret files (if you accidentally created them)
talsecret.yaml
secrets.yaml

# Keep the encrypted file, ignore others
!talsecret.sops.yaml

# --- Terraform ---
# Local .terraform directory (providers/modules)
.terraform/

# State files (contain sensitive data about your infra)
*.tfstate
*.tfstate.backup

# Var files (if you ever use them locally)
*.tfvars

# Crash logs
crash.log

# --- Talos / Talhelper ---
# The output directory from 'talhelper genconfig'
# It contains your node manifests and, crucially, your admin 'talosconfig'.
# Since you can regenerate this from talconfig.yaml + sops, do not commit it.
clusterconfig/

# The kubeconfig file generated by Terraform
kubeconfig
kubeconfig.yaml

# --- IDE & OS ---
.DS_Store
.vscode/
.idea/
out.plan
.env
repomix-output.xml